apiVersion: apps/v1
kind: Deployment
metadata:
    name: kloudstack-compute-controller
    namespace: hicompute
    labels:
        app: kloudstack-compute-controller
spec:
    replicas: 1
    selector:
        matchLabels:
            app: kloudstack-compute-controller
    template:
        metadata:
            labels:
                app: kloudstack-compute-controller
        spec:
            serviceAccountName: kloudstack-compute-controller
            affinity:
                nodeAffinity:
                    requiredDuringSchedulingIgnoredDuringExecution:
                        nodeSelectorTerms:
                            - matchExpressions:
                                  - key: node-role.kubernetes.io/control-plane
                                    operator: Exists
            tolerations:
                - key: node-role.kubernetes.io/control-plane
                  operator: Exists
                  effect: NoSchedule
            containers:
                - name: kloudstack-compute-controller
                  image: registry.ik8s.ir/hicompute/kloudstack:v0.0.0-alpha.0
                  imagePullPolicy: Always
                  args:
                      - "controller"
                  resources:
                      requests:
                          memory: "32Mi"
                          cpu: "100m"
                      limits:
                          memory: "128Mi"
                          cpu: "200m"
