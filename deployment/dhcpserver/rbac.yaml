apiVersion: v1
kind: ServiceAccount
metadata:
    name: dhcp-server
    namespace: kloudstack
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    name: kloudstack:dhcp-server
rules:
    - apiGroups:
          - ipam.kloudstack.ir
      resources:
          - clusterips
          - clusterippools
      verbs:
          - get
          - list
    - apiGroups:
          - ipam.histack.ir
      resources:
          - clusterips/status
          - clusterippools/status
      verbs:
          - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
    name: kloudstack:dhcp-server
roleRef:
    apiGroup: rbac.authorization.k8s.io
    kind: ClusterRole
    name: kloudstack:dhcp-server
subjects:
    - kind: ServiceAccount
      name: dhcp-server
      namespace: hicompute
