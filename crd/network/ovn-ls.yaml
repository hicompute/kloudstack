---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
    name: logicalswitches.network.kloudstack.ir
spec:
    group: network.kloudstack.ir
    names:
        kind: LogicalSwitch
        listKind: LogicalSwitchList
        plural: logicalswitches
        singular: logicalswitch
    scope: Namespaced
    versions:
        - name: v1alpha1
          schema:
              openAPIV3Schema:
                  properties:
                      apiVersion:
                          description: |-
                              APIVersion defines the versioned schema of this representation of an object.
                              Servers should convert recognized schemas to the latest internal value, and
                              may reject unrecognized values.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                          type: string
                      kind:
                          description: |-
                              Kind is a string value representing the REST resource this object represents.
                              Servers may infer this from the endpoint the client submits requests to.
                              Cannot be updated.
                              In CamelCase.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                      metadata:
                          type: object
                      status:
                          description: status defines the observed state of ClusterIPPool
                          properties:
                              ports:
                                  description: ports defines the ports of the ClusterIPPool
                                  type: array
                                  items:
                                      type: string
                          type: object
                  type: object
          served: true
          storage: true
          subresources:
              status: {}
